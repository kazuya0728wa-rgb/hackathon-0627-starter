# 設計判断ログ

> チーム名：E

---

## 判断ログ一覧

### [ADR-001] register() を責務ごとにクラス分割した

**日時**：2026-06-27

**状況**：1メソッドに検証・重複確認・ハッシュ化・保存・送信・ログの6責務が密集し、変更もテストも困難だった。

**決定**：各責務を `UserValidator`／`UserRepository`／`PasswordHasher`／`EmailService`／`AuditLogger` に分離し、`register()` はそれらを順に呼ぶだけのオーケストレーションにした。

**理由**：単一責任の原則により、変更理由をクラス単位に閉じ込め、各責務を独立してテスト・再利用できるようにするため。

**トレードオフ**：クラス数が増え、この規模だけ見ると冗長。だが拡張・テスト容易性を優先した。

---

### [ADR-002] 依存を new 直書きからコンストラクタDIに変えた

**日時**：2026-06-27

**状況**：`new Database()` / `new EmailClient()` をフィールドで生成しており、テストで本物が動いてしまう。

**決定**：依存をインターフェース化し、コンストラクタで注入する形にした。既存呼び出し互換のため、デフォルト構成を組む無引数コンストラクタも残した。

**理由**：モック注入で純粋な単体テストを可能にし、本番/検証など環境別の実装差し替えを容易にするため。

**トレードオフ**：DIコンテナは導入せず手動配線にしたため、Composition Root の組み立てが煩雑になる。規模的に手動で十分と判断した。

---

### [ADR-003] 例外を型分けし、throws Exception をやめた

**日時**：2026-06-27

**状況**：全エラーが `IllegalArgumentException`、メソッドは `throws Exception`。呼び出し側がエラー種別を区別できなかった。

**決定**：`RegistrationException` を基底に `ValidationException`（400相当）／`DuplicateUserException`（409相当）を定義。非チェック例外にして `throws` を不要にした。

**理由**：呼び出し側がHTTPステータスに振り分けられるようにし、業務エラーとシステム障害を分離するため。

**トレードオフ**：例外クラスが増える。また非チェック例外は握り忘れリスクがあるが、Web層での集約ハンドリングを前提に許容した。

---

### [ADR-004] ダミー実装（偽ハッシュ化）を1クラスに隔離した

**日時**：2026-06-27

**状況**：`password + "_hashed"` がサービス本体に直書きされていた。

**決定**：`PasswordHasher` インターフェースを切り出し、ダミーを `FakePasswordHasher` に閉じ込めた。

**理由**：本ハッシュ化（bcrypt/Argon2）への差し替えを `BcryptPasswordHasher` の追加だけで完結させ、本体を無改修にするため。

**トレードオフ**：今回はセキュリティを本実装せず後回しにした。実害リスクは認識しているが、ハッカソンの評価軸（設計）を優先した。

---

### [ADR-005] プロバイダ固有の箱を「コード→正規化プロフィール」だけに限定した

**日時**：2026-06-27

**状況**：GitHubに続きGoogle・LINEを足したい。固有処理の境界をどこに引くかが、拡張容易性を決める核心だった。

**決定**：`OAuthProvider` の責務を「認可コード → 正規化プロフィール（`OAuthUserProfile`）」のみに絞り、ユーザー作成・永続化・セッション発行・監査は共有コアに置いた。

**理由**：プロバイダ追加時に共通ロジックをコピペさせないため。境界を最小化することで、追加コストを「1クラス＋登録1行」に抑えられる。

**トレードオフ**：正規化プロフィールが各社の差異を吸収しきれない項目（例：LINEはメールが任意）は、別途プロバイダ側で補う必要がある。

---

### [ADR-006] find-or-create を UserRegistrar に共有化した

**日時**：2026-06-27

**状況**：パスワード登録とソーシャルログインの両方が「ユーザーを作る」処理を持つ。

**決定**：`UserRegistrar.findOrCreateBySocial()` に集約し、両入口から呼ぶ構成にした。

**理由**：ユーザー生成ロジックの二重化を防ぎ、登録ポリシーの変更を1箇所に閉じ込めるため。

**トレードオフ**：自動登録の可否など登録ポリシーが入口ごとに異なる場合は、将来分岐が必要になる。

---

### [ADR-007] ソーシャル連携を単純1:1にした

**日時**：2026-06-27

**状況**：1ユーザーに複数SNSを紐付ける（1:N）か、1プロバイダ固定（1:1）かを選ぶ必要があった。

**決定**：今回は `User` が `provider`／`providerUserId` を1つ持つ単純1:1とした。

**理由**：ハッカソンの時間内で設計意図を明確に示すには、複雑さを抑えた方がよいと判断したため。

**トレードオフ**：1ユーザーが複数SNSでログインして同一アカウントになる要件は満たせない。実現には `UserIdentity` の別エンティティ分離が必要（→ DESIGN.md §5）。
